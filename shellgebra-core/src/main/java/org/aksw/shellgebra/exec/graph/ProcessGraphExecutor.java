package org.aksw.shellgebra.exec.graph;

public class ProcessGraphExecutor {
    // group, pipeline, exec, inputs...
    public void exec(ProcessGraph graph) {
        // { cat /tmp/fun | tee /tmp/fun ; } < /tmp/fun

        // TopologicalOrderIterator<V, E>

    }
}
